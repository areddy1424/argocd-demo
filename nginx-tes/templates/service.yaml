apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-tes.fullname" . }}
  labels:
    {{- include "nginx-tes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-tes.selectorLabels" . | nindent 4 }}
